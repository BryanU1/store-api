extends layout

block content
  h1= title
  p Welcome to #[em KeyB]

  h1 Dynamic content

  if error
    p Error getting dynamic content.
  else
    p The store has the following record counts:

    ul
      li #[strong Items:] !{data.item_count}
      li #[strong Categories:] !{data.category_count}